| fixture.nix:8:5:8:14 | fixupPhase | This derivation overrides `fixupPhase` directly. Keep stdenv's default `fixupPhase` and add custom commands in `preFixup` or `postFixup` instead. |
| fixture.nix:17:5:17:14 | patchPhase | This derivation overrides `patchPhase` directly. Keep stdenv's default `patchPhase` and add custom commands in `prePatch` or `postPatch` instead. |
| fixture.nix:59:5:59:14 | fixupPhase | This derivation overrides `fixupPhase` directly. Keep stdenv's default `fixupPhase` and add custom commands in `preFixup` or `postFixup` instead. |
